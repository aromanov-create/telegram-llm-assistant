[Unit]
Description=SSH SOCKS5 Proxy Tunnel
After=network.target

[Service]
Type=simple
ExecStart=/usr/bin/ssh -N -D 1080 -o "ServerAliveInterval 60" -o "ExitOnForwardFailure yes" -o "StrictHostKeyChecking no" your-ssh-host-alias
Restart=always
RestartSec=10

[Install]
WantedBy=default.target
